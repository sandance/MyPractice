
def heapsort(aList):
	length = len(aList) -1
	Parent = length/2
	
	for i in xrange(Parent, -1, -1):
		print i, aList[i]
		#	tickledown(aList, i, length)


#def tickledown(aList, first , last):
#	largest =i

heapsort([5,6,7,2,3])
 	
